export const metadata = {
  title: "Posts | Articls",
  description: "Discover new posts and articles",
};
const postsLayout = ({ children }) => {
  return <div>{children}</div>;
};

export default postsLayout;
